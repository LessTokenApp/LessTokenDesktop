# Install the local package into a virtual environment.
-e .

# Core desktop features.
pyperclip>=1.9.0
Pillow>=10.0
requests>=2.31.0  # Auto-update system
packaging>=23.0  # Version parsing

# Web server (Flask backend).
Flask>=3.0.0
Flask-CORS>=4.0.0
python-dotenv>=1.0.0
gunicorn>=21.0.0  # Production WSGI server

# Optional AI providers (multi-provider support).
openai>=1.0
anthropic>=0.17.0
google-generativeai>=0.3.0
ollama>=0.1.0
tiktoken>=0.5.0

# Optional file processing.
pypdf>=4.0
python-docx>=1.1

# Optional OCR support.
pytesseract>=0.3

# Windows-specific features.
pywin32>=306; platform_system == "Windows"
